# 英会話フレーズ反復学習ツール 設計書

## 1. 目的とゴール

- **目的**: 日常英会話で使える文章(フレーズ)を繰り返し学習し、記憶に定着させる。
- **ゴール**: 利用開始から **2ヶ月間で、複雑ではない日常英会話ができる状態** になること。
  - 具体的な到達イメージ: あいさつ・自己紹介・買い物・レストラン・道案内・スモールトークなど、定型的な場面で即座に口から英文が出てくる。
- **ユーザー**: 本ツールの利用者は自分1人(シングルユーザー)。サーバーやアカウント管理は不要。

## 2. コンセプト

**「文章(センテンス)単位の反復 × 間隔反復(SRS) × 音読」**

単語ではなく「そのまま使える文章」を学習単位にする。忘却曲線に基づく間隔反復で復習タイミングを自動管理し、毎日短時間(15〜20分)続けるだけで2ヶ月後に約500文が定着する設計とする。

## 3. 技術方針

シンプルさと継続しやすさを最優先する。

| 項目 | 選定 | 理由 |
|---|---|---|
| 形態 | 単一HTMLファイルのWebアプリ(ビルド不要) | ブラウザで開くだけで使える。スマホでも動く。GitHub Pagesでそのまま公開可能 |
| 言語 | HTML + CSS + Vanilla JavaScript | 依存ゼロ。学習・保守が容易 |
| データ保存 | ブラウザの localStorage | サーバー不要。学習進捗・追加フレーズを端末内に永続化 |
| 音声 | Web Speech API(speechSynthesis) | 追加ライブラリなしで英文の読み上げ(発音確認)ができる |
| デフォルト文章 | JSアレイとしてアプリに同梱(`sentences.js` 相当) | オフラインでも初回から使える |

バックアップ用に、学習データ(追加文章+進捗)を JSON でエクスポート/インポートできるようにする(localStorage消失対策)。

## 4. 学習フロー(2ヶ月カリキュラム)

### 4.1 1日の学習サイクル(15〜20分)

1. **復習**: SRSが「今日復習すべき」と判定した文章を出題(平均20〜40文)
2. **新規学習**: 新しい文章を1日 **10文** 学習
3. 各文章の学習ステップ:
   - 日本語訳を見る → 英文を思い出して口に出す(想起練習)
   - 「答えを見る」で英文を表示
   - 🔊ボタンで音声を聞き、**3回音読**(シャドーイング)
   - 自己評価ボタンで回答: 「わからなかった / 微妙 / できた」

### 4.2 2ヶ月の進行

- 10文/日 × 60日 = **600文**(デフォルト収録は約550文+任意追加分)
- 週構成の目安:

| 週 | カテゴリ |
|---|---|
| 1週目 | あいさつ・自己紹介・基本の受け答え |
| 2週目 | 依頼・質問・許可(Can you〜? / Could I〜?) |
| 3週目 | 買い物・レストラン |
| 4週目 | 道案内・交通・旅行 |
| 5週目 | 予定・時間・日常の行動描写 |
| 6週目 | 感情・意見・感想(I think〜 / I feel〜) |
| 7週目 | スモールトーク(天気・週末・趣味) |
| 8週目 | トラブル対応・電話・総復習 |

### 4.3 間隔反復アルゴリズム(簡易ライトナー方式)

各文章は Box 0〜5 のいずれかに属する。自己評価に応じて移動する。

- **できた** → Boxが1つ上がる
- **微妙** → Boxそのまま(当日中にもう一度出題)
- **わからなかった** → Box 0 に戻る(当日中にもう一度出題)

Boxごとの復習間隔:

| Box | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| 次回まで | 当日 | 1日 | 3日 | 7日 | 14日 | 30日(定着扱い) |

Box 5 を2回連続「できた」なら **習得済み(mastered)** としてカウント。

## 5. 機能一覧

### 5.1 MVP(最初に作る範囲)

1. **今日の学習** — 復習+新規10文を上記サイクルで出題(日本語→英語の想起型カード)
2. **音声読み上げ** — 英文をネイティブ発音で再生(速度 0.7x / 1.0x 切替)
3. **自己評価とSRS** — 3段階評価でBoxを更新し、次回復習日を自動計算
4. **文章の追加・編集・削除** — 英文・日本語訳・カテゴリを入力して任意の文章を登録。追加文も同じSRSに乗る
5. **デフォルト文章セット** — 約550文をカテゴリ付きで同梱(§4.2 の8カテゴリ)
6. **進捗ダッシュボード** — 学習日数・連続日数(ストリーク)・習得済み文数・2ヶ月ゴールまでの進捗バー
7. **データのエクスポート/インポート**(JSON)

### 5.2 将来拡張(MVP後)

- 音声認識(SpeechRecognition)による発音チェック
- 英→日方向の出題、穴埋め(キーフレーズをブランクに)モード
- カテゴリ指定の集中練習モード
- 通知・リマインダー(PWA化)

## 6. 画面設計

SPA内のタブ切替で3画面。

```
┌──────────────────────────────┐
│  📚 毎日英会話   [学習] [文章一覧] [進捗]   │
├──────────────────────────────┤
│ 学習画面                                    │
│  残り: 復習 12 / 新規 10                    │
│  ┌────────────────────────┐  │
│  │ カテゴリ: レストラン                │  │
│  │                                    │  │
│  │  「おすすめは何ですか?」            │  │
│  │                                    │  │
│  │        [ 答えを見る ]               │  │
│  └────────────────────────┘  │
│  ── 答え表示後 ──                          │
│  │ What do you recommend?   🔊 再生    │  │
│  │ [わからなかった] [微妙] [できた]     │  │
└──────────────────────────────┘
```

- **文章一覧画面**: カテゴリでフィルタできる一覧+「+ 文章を追加」フォーム(英文 / 日本語訳 / カテゴリ)。各行に編集・削除・Box状態表示。
- **進捗画面**: ストリーク、今日の完了状況、習得済み/学習中/未着手の内訳、60日ゴールへの進捗バー、エクスポート/インポートボタン。

## 7. データモデル

```js
// 文章(デフォルトはコード同梱、追加分は localStorage)
Sentence {
  id: string,          // "d-001"(デフォルト) / "u-<timestamp>"(ユーザー追加)
  en: string,          // "What do you recommend?"
  ja: string,          // "おすすめは何ですか?"
  category: string,    // "restaurant" など8カテゴリ+ "custom"
  isCustom: boolean
}

// 学習進捗(localStorage: key = sentence id)
Progress {
  box: number,         // 0-5
  nextReview: string,  // "2026-09-03"(ISO日付)
  correctStreak: number,
  introduced: boolean  // 新規学習済みか
}

// アプリ状態(localStorage)
AppState {
  startDate: string,       // 利用開始日(60日進捗の基準)
  lastStudyDate: string,
  streak: number,
  newPerDay: number        // 既定 10(設定で変更可)
}
```

localStorage キー: `eng.sentences.custom` / `eng.progress` / `eng.state`

## 8. ファイル構成

```
/
├── index.html      # アプリ本体(HTML + CSS + JS を1ファイルに同梱)
├── DESIGN.md       # 本設計書
└── README.md
```

※ デフォルト文章550文は `index.html` 内の JS 配列として持つ(1ファイル運用を維持)。文章量が増えて見通しが悪くなったら `sentences.js` に分離する。

## 9. 主要ロジック(擬似コード)

```js
// 今日の出題キューを作る
function buildTodayQueue() {
  const due = allSentences.filter(s =>
    progress[s.id]?.introduced && progress[s.id].nextReview <= today());
  const fresh = allSentences.filter(s => !progress[s.id]?.introduced)
                            .slice(0, state.newPerDay);
  return shuffle(due).concat(fresh);
}

// 自己評価の反映
const INTERVALS = [0, 1, 3, 7, 14, 30]; // Box→日数
function grade(id, result) { // result: 'ng' | 'soso' | 'ok'
  const p = progress[id];
  if (result === 'ok')      p.box = Math.min(p.box + 1, 5), p.correctStreak++;
  else if (result === 'soso') requeueToday(id);            // Box据え置き
  else                       p.box = 0, p.correctStreak = 0, requeueToday(id);
  p.nextReview = addDays(today(), INTERVALS[p.box]);
  p.introduced = true;
  save();
}
```

## 10. 実装ロードマップ

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| 1 | 学習画面+SRS+localStorage(デフォルト50文で動作確認) | 1日分の学習サイクルが回る |
| 2 | 音声読み上げ・文章追加/編集/削除・一覧画面 | 任意文章を追加してSRSに乗せられる |
| 3 | デフォルト文章を550文に拡充(8カテゴリ) | 60日カリキュラム分の文章が揃う |
| 4 | 進捗画面・ストリーク・エクスポート/インポート | 2ヶ月の進捗が可視化される |
| 5 | (任意)GitHub Pages公開・スマホ表示最適化 | スマホで毎日使える |

## 11. 設計上の判断メモ

- **サーバーレス(localStorage)にした理由**: ユーザーが1人で、共有・同期の要件がないため。導入の手間ゼロを優先。データ消失リスクはJSONエクスポートで担保。
- **単語ではなく文章単位**: 会話は「型」の想起速度が命。文章ごと覚えれば文法・語彙・語順を同時に習得できる。
- **想起(日→英)方向をデフォルト**: 会話で必要なのは「言いたいこと→英文」の変換。読解方向(英→日)は易しすぎて定着効果が薄い。
- **1日新規10文**: 復習負荷が積み上がるため、これ以上増やすと1日20分を超えて継続率が下がる。60日×10文でゴールの600文に到達できる。
