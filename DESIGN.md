# 英語研修対策 フレーズ反復学習ツール 設計書

## 1. 目的とゴール

- **背景**: 2ヶ月後に社内研修があり、基本的に英語で進行される。アジア圏の同僚とチームを組むグループワークもある。
- **目的**: 研修で「困らない」状態を作る。具体的には:
  1. **聞き取れなかった時に会話を止めずにリカバリーできる**(聞き返し・確認)
  2. **グループワークで黙り込まずに参加できる**(意見・賛成/反対・提案・役割分担)
  3. **自己紹介・報告・発表の定型がすぐ口から出る**
- **ゴール設定の考え方**: 研修は「流暢さ」ではなく「参加できること」が合格ライン。完璧な英文より、会話を回すための定型フレーズの即答性を最優先する。
- **ユーザー**: 本ツールの利用者は自分1人(シングルユーザー)。サーバー・アカウント管理は不要。

## 2. コンセプト

**「研修シーン別の実用フレーズ × 間隔反復(SRS) × 音読」**

単語ではなく「その場面でそのまま使える文章」を学習単位にする。忘却曲線に基づく間隔反復で復習タイミングを自動管理し、毎日15〜20分で2ヶ月後に約550文を定着させる。

日常会話版との最大の違いは **優先順位**:
- 「聞き返し・確認」を最初の週に置く。これが身につけば、以降の研修中も実地で学び続けられる(会話が止まらない=学習機会が続く)。
- 買い物・道案内などの旅行系フレーズは収録しない。研修1日の流れ(朝の挨拶→セッション→グループワーク→休憩の雑談→発表→振り返り)を全カテゴリでカバーする。

## 3. 技術方針

(前提が変わっても最適解は同じため、技術構成は維持)

| 項目 | 選定 | 理由 |
|---|---|---|
| 形態 | 単一HTMLファイルのWebアプリ(ビルド不要) | ブラウザで開くだけ。スマホでも動くので通勤中に使える |
| 言語 | HTML + CSS + Vanilla JavaScript | 依存ゼロ。学習・保守が容易 |
| データ保存 | ブラウザの localStorage | サーバー不要。進捗・追加フレーズを端末内に永続化 |
| 音声 | Web Speech API(speechSynthesis) | 追加ライブラリなしで読み上げ。0.7x〜1.2x の速度切替 |
| デフォルト文章 | JSアレイとしてアプリに同梱 | オフラインでも初回から使える |

バックアップ用に、学習データ(追加文章+進捗)の JSON エクスポート/インポートを用意する。

**リスニング対策の補足**: アジア圏の同僚との会話では発音のバリエーションが大きい。ツール側では (1) 再生速度を上げた聞き取り練習(1.2x)、(2) ブラウザが持つ複数の英語音声(en-US / en-GB / en-IN など)をランダムに切り替えるオプション、で「同じ文を違う音で聞く」訓練を入れる。

## 4. コンテンツ構成(8カテゴリ・約550文)

研修での重要度順。**サバイバル度 = これがないと詰まる度合い**。

| # | カテゴリ | 例 | サバイバル度 | 文数目安 |
|---|---|---|---|---|
| 1 | **聞き返し・確認** | Sorry, could you say that again? / Do you mean 〜? / Let me make sure I understand. | ★★★ | 60 |
| 2 | **自己紹介・アイスブレイク** | I'm in charge of 〜. / I've been with the company for 〜. / Nice to be working with you. | ★★★ | 60 |
| 3 | **意見・賛成・反対** | I think 〜 because 〜. / That's a good point. / I see it a little differently. | ★★★ | 80 |
| 4 | **提案・役割分担(グループワーク)** | Why don't we 〜? / I can take care of 〜. / Shall we split the tasks? | ★★★ | 80 |
| 5 | **質問・依頼・お願い** | Could you give me an example? / Would you mind sharing your screen? | ★★ | 60 |
| 6 | **進捗報告・発表** | Let me walk you through 〜. / We decided to 〜. / To sum up, 〜. | ★★ | 70 |
| 7 | **休憩中の雑談(同僚向けスモールトーク)** | How's the training going for you? / Where are you based? / Have you been to Japan before? | ★★ | 70 |
| 8 | **会議進行・時間・トラブル対応** | We're running out of time. / Can everyone hear me? / Sorry to interrupt, but 〜. | ★ | 70 |

- 各文章には **場面タグ**(発表 / 議論 / 休憩 など)も付け、「明日は発表がある」という日に集中練習できるようにする。
- 任意の文章も追加可能(§6)。研修の資料や案内メールに出てきた表現をその場で登録し、同じSRSに乗せる。

## 5. 学習フロー(2ヶ月カリキュラム)

### 5.1 1日の学習サイクル(15〜20分)

1. **復習**: SRSが「今日復習すべき」と判定した文章を出題(平均20〜40文)
2. **新規学習**: 新しい文章を1日 **10文**
3. 各文章の学習ステップ:
   - 日本語を見る → 英文を思い出して**口に出す**(想起練習)
   - 「答えを見る」で英文表示 → 🔊で音声を聞き **3回音読**
   - 自己評価: 「わからなかった / 微妙 / できた」

### 5.2 8週間の進行(研修から逆算)

| 週 | テーマ | 内容 |
|---|---|---|
| 1 | サバイバル | カテゴリ1(聞き返し・確認)+ カテゴリ2(自己紹介)前半 |
| 2 | 自分のことを話す | カテゴリ2後半 + カテゴリ5(質問・依頼)前半 |
| 3 | 議論に入る | カテゴリ3(意見・賛成・反対)前半 |
| 4 | 議論を深める | カテゴリ3後半 + カテゴリ5後半 |
| 5 | グループワーク | カテゴリ4(提案・役割分担) |
| 6 | 報告・発表 | カテゴリ6(進捗報告・発表) |
| 7 | 雑談・関係構築 | カテゴリ7(スモールトーク) |
| 8 | 仕上げ | カテゴリ8(進行・トラブル対応)+ ★★★カテゴリの総復習 |

- 8週目は新規を5文/日に減らし、浮いた時間をサバイバル度★★★の総復習と「チートシートモード」(§6)の通し読みに充てる。
- 10文/日 × 約55日 = 550文で全カテゴリを消化できる計算。

### 5.3 間隔反復アルゴリズム(簡易ライトナー方式)

各文章は Box 0〜5 のいずれかに属し、自己評価で移動する。

- **できた** → Boxが1つ上がる
- **微妙** → Box据え置き(当日中に再出題)
- **わからなかった** → Box 0 に戻る(当日中に再出題)

| Box | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| 次回まで | 当日 | 1日 | 3日 | 7日 | 14日 | 30日(定着扱い) |

Box 5 を2回連続「できた」なら **習得済み(mastered)**。

## 6. 機能一覧

### 6.1 MVP

1. **今日の学習** — 復習+新規10文(日本語→英語の想起型カード)
2. **音声読み上げ** — 速度 0.7x / 1.0x / 1.2x。英語音声の種類をランダム切替するオプション(多様な発音への耐性づくり)
3. **自己評価とSRS** — 3段階評価でBoxを更新し、次回復習日を自動計算
4. **文章の追加・編集・削除** — 英文・日本語訳・カテゴリ・場面タグを登録。追加文も同じSRSに乗る
5. **デフォルト文章セット** — 研修シーン特化の約550文(§4の8カテゴリ)
6. **カテゴリ集中モード** — カテゴリ/場面タグを指定して出題(「明日は発表」前日の駆け込み練習用)。SRSとは独立で、Boxは変更しない
7. **チートシートモード** — カテゴリごとの全フレーズを一覧表示する印刷向け画面。研修当日にスマホで見る/紙で持ち込む用
8. **進捗ダッシュボード** — 研修日までのカウントダウン、学習日数・ストリーク・習得済み文数・カテゴリ別の定着率
9. **データのエクスポート/インポート**(JSON)

### 6.2 将来拡張(MVP後)

- 音声認識(SpeechRecognition)による発音チェック
- ミニダイアログモード(2〜4往復の会話形式で出題。議論の「流れ」の練習)
- 穴埋めモード(キーフレーズをブランクに)
- PWA化・リマインダー通知

## 7. 画面設計

SPA内のタブ切替で4画面(学習 / 文章一覧 / チートシート / 進捗)。

```
┌──────────────────────────────┐
│ 📚 研修英語   [学習] [一覧] [シート] [進捗] │
├──────────────────────────────┤
│ 研修まであと 42日   残り: 復習12 / 新規10   │
│  ┌────────────────────────┐  │
│  │ カテゴリ: 聞き返し・確認            │  │
│  │                                    │  │
│  │ 「もう少しゆっくり話して            │  │
│  │   いただけますか?」                │  │
│  │        [ 答えを見る ]               │  │
│  └────────────────────────┘  │
│  ── 答え表示後 ──                          │
│  │ Could you speak a little more       │  │
│  │ slowly, please?      🔊 0.7x/1x/1.2x│  │
│  │ [わからなかった] [微妙] [できた]     │  │
└──────────────────────────────┘
```

- **文章一覧**: カテゴリ・場面タグでフィルタ+「+ 追加」フォーム。各行に編集・削除・Box表示。「このカテゴリを集中練習」ボタン。
- **チートシート**: カテゴリ→フレーズの一覧(日英併記)。印刷用CSS対応。
- **進捗**: 研修日カウントダウン、ストリーク、カテゴリ別定着率バー(★★★カテゴリの定着が遅れていれば強調表示)、エクスポート/インポート。

## 8. データモデル

```js
// 文章(デフォルトはコード同梱、追加分は localStorage)
Sentence {
  id: string,          // "d-001"(デフォルト) / "u-<timestamp>"(ユーザー追加)
  en: string,          // "Could you say that again?"
  ja: string,          // "もう一度言っていただけますか?"
  category: string,    // §4 の8カテゴリ + "custom"
  scenes: string[],    // 場面タグ: "discussion" | "presentation" | "break" など
  priority: number,    // サバイバル度 1-3(進捗画面の強調・総復習の優先度に使用)
  isCustom: boolean
}

// 学習進捗(localStorage: key = sentence id)
Progress {
  box: number,         // 0-5
  nextReview: string,  // "2026-09-03"(ISO日付)
  correctStreak: number,
  introduced: boolean
}

// アプリ状態(localStorage)
AppState {
  startDate: string,
  trainingDate: string,    // 研修日(カウントダウンの基準。初回起動時に入力)
  lastStudyDate: string,
  streak: number,
  newPerDay: number,       // 既定 10
  voiceVariety: boolean    // 音声ランダム切替
}
```

localStorage キー: `eng.sentences.custom` / `eng.progress` / `eng.state`

## 9. ファイル構成

```
/
├── index.html      # アプリ本体(HTML + CSS + JS を1ファイルに同梱)
├── DESIGN.md       # 本設計書
└── README.md
```

※ デフォルト550文は `index.html` 内のJS配列として持つ。見通しが悪くなったら `sentences.js` に分離。

## 10. 主要ロジック(擬似コード)

```js
// 今日の出題キューを作る
function buildTodayQueue() {
  const due = allSentences.filter(s =>
    progress[s.id]?.introduced && progress[s.id].nextReview <= today());
  const fresh = allSentences.filter(s => !progress[s.id]?.introduced)
                            .slice(0, state.newPerDay);
  return shuffle(due).concat(fresh);   // 新規はカリキュラム順、復習はシャッフル
}

// 自己評価の反映
const INTERVALS = [0, 1, 3, 7, 14, 30]; // Box→日数
function grade(id, result) { // 'ng' | 'soso' | 'ok'
  const p = progress[id];
  if (result === 'ok')        p.box = Math.min(p.box + 1, 5), p.correctStreak++;
  else if (result === 'soso') requeueToday(id);
  else                        p.box = 0, p.correctStreak = 0, requeueToday(id);
  p.nextReview = addDays(today(), INTERVALS[p.box]);
  p.introduced = true;
  save();
}

// カテゴリ集中モード(SRSに影響しない)
function drillMode(filter) {
  return shuffle(allSentences.filter(s =>
    s.category === filter.category ||
    s.scenes.includes(filter.scene)));
}
```

## 11. 実装ロードマップ

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| 1 | 学習画面+SRS+localStorage(カテゴリ1の60文で動作確認) | 1日分の学習サイクルが回る |
| 2 | 音声読み上げ(速度・音声切替)・文章追加/編集/削除・一覧画面 | 任意文章を追加してSRSに乗せられる |
| 3 | デフォルト文章を550文に拡充(8カテゴリ+場面タグ) | 8週間カリキュラム分が揃う |
| 4 | 集中モード・チートシート・進捗画面(カウントダウン含む) | 研修前日の駆け込み練習と当日持ち込みができる |
| 5 | (任意)GitHub Pages公開・スマホ最適化 | 通勤中にスマホで毎日使える |

フェーズ1〜3を最優先。**研修日が固定なので、ツール作りに時間をかけすぎないこと**(フェーズ1完成時点=開始2〜3日目から学習を始められる構成にしている)。

## 12. 設計上の判断メモ

- **「聞き返し・確認」を最初に置く理由**: 研修で最も高頻度かつ、詰まった時の保険になる。これがあれば残り7週間の学習が研修本番でも続けられる。
- **旅行系フレーズを外した理由**: 60日×10文の枠は有限。研修1日の流れに出てこない場面は収録しない。
- **チートシートモードを足した理由**: 学習ツールとしてはSRSが本体だが、「当日のお守り」があると心理的な余裕が生まれ、実際の発話量が増える。
- **音声のランダム切替**: アジア圏の同僚の英語は音のバリエーションが広い。「一種類の模範音声だけで覚えた文が聞き取れない」事態を減らすため、同じ文を複数の音・速度で聞かせる。
- **想起(日→英)方向がデフォルト**: グループワークで必要なのは「言いたいこと→英文」の即時変換。
- **サーバーレス(localStorage)**: ユーザー1人・共有要件なし。導入の手間ゼロを優先し、消失リスクはJSONエクスポートで担保。
