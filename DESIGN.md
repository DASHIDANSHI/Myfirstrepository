# 英語研修+懇親会対策 フレーズ反復学習ツール 設計書

## 1. 目的とゴール

- **背景**:
  - **研修は2026年11月2日(月)〜4日(水)の3日間**、基本的に英語で進行される。学習開始(9月2日)から研修初日まで **61日**。
  - アジア太平洋圏の同僚とチームを組むグループワークがある。
  - **研修の夜には飲み会・懇親会パーティがあり、そこにも参加したい。** 3日間開催のため、夜の懇親会は複数回、また2日目以降は「前日会った人との再会」から1日が始まる。
  - 参加者は **オーストラリア・ニュージーランド・中国・香港・インド・台湾・韓国・インドネシア・日本** から選抜された **約50人**。
- **目的**: 研修と懇親会の両方で「困らない」状態を作る。具体的には:
  1. **聞き取れなかった時に会話を止めずにリカバリーできる**(聞き返し・確認)
  2. **グループワークで黙り込まずに参加できる**(意見・賛成/反対・提案・役割分担)
  3. **自己紹介・報告・発表の定型がすぐ口から出る**
  4. **懇親会で輪に入り、雑談を続け、気持ちよく切り上げられる**
- **ゴール設定の考え方**: 合格ラインは「流暢さ」ではなく「参加できること」。完璧な英文より、会話を回す定型フレーズの即答性を最優先する。
- **ユーザー**: 本ツールの利用者は自分1人(シングルユーザー)。サーバー・アカウント管理は不要。

## 2. コンセプト

**「研修+懇親会のシーン別実用フレーズ × 間隔反復(SRS) × 音読」**

単語ではなく「その場面でそのまま使える文章」を学習単位にする。忘却曲線に基づく間隔反復で復習タイミングを自動管理し、毎日15〜20分で2ヶ月後に約560文を定着させる。

優先順位の設計:
- 「聞き返し・確認」を最初の週に置く。これが身につけば研修中も実地で学び続けられる(会話が止まらない=学習機会が続く)。
- 50人規模なので**自己紹介は何度も繰り返す**ことになる。序盤で固め、名前を聞き返す・覚えるフレーズも厚めに入れる。
- 懇親会は独立カテゴリとして本格的に扱う(おまけ扱いにしない)。日中のワークより聞き取り環境が悪く(騒がしい・カジュアル速度)、話題も広いため、専用の練習が必要。
- 旅行系フレーズ(買い物・道案内)は収録しない。研修1日の流れ(朝の挨拶→セッション→グループワーク→休憩→発表→懇親会)を全カテゴリでカバーする。

## 3. 技術方針

| 項目 | 選定 | 理由 |
|---|---|---|
| 形態 | 単一HTMLファイルのWebアプリ(ビルド不要) | ブラウザで開くだけ。スマホでも動くので通勤中に使える |
| 言語 | HTML + CSS + Vanilla JavaScript | 依存ゼロ。学習・保守が容易 |
| データ保存 | ブラウザの localStorage | サーバー不要。進捗・追加フレーズを端末内に永続化 |
| 音声 | Web Speech API(speechSynthesis) | 追加ライブラリなしで読み上げ。速度・音声の切替が可能 |
| デフォルト文章 | JSアレイとしてアプリに同梱 | オフラインでも初回から使える |

バックアップ用に、学習データ(追加文章+進捗)の JSON エクスポート/インポートを用意する。

### 3.1 リスニング対策(参加国を踏まえた音声設計)

参加者の英語は米国標準音声とは大きく異なる。ブラウザのTTSで再現できる範囲で「同じ文を違う音で聞く」訓練を組み込む。

- **利用する音声**: en-AU(豪)/ en-NZ(NZ、無ければen-AUで代用)/ en-IN(印)/ en-GB(香港・シンガポール系英語に近い基準として)/ en-US。端末にある音声から自動検出し、ランダム切替オプションで再生ごとに変える。
- **速度切替**: 0.7x / 1.0x / 1.2x。1.2x は「騒がしい懇親会で断片しか聞こえない」状況への耐性づくりも兼ねる。
- **限界と補完**: 中国・韓国・インドネシア訛りの英語はTTSでは再現できない。ツールの範囲外だが、チートシート画面に「参加国の英語に耳を慣らすための聞き流し素材の探し方」を短いメモとして載せる(例: 各国スピーカーのTEDトーク)。

## 4. コンテンツ構成(9カテゴリ・約560文)

研修+懇親会での重要度順。**サバイバル度 = これがないと詰まる度合い**。

| # | カテゴリ | 例 | サバイバル度 | 文数 |
|---|---|---|---|---|
| 1 | **聞き返し・確認** | Sorry, could you say that again? / Do you mean 〜? / It's a bit noisy — could you speak up? | ★★★ | 60 |
| 2 | **自己紹介・名前・アイスブレイク** | I'm in charge of 〜. / Sorry, could I get your name again? / How do I pronounce your name? | ★★★ | 60 |
| 3 | **意見・賛成・反対** | I think 〜 because 〜. / That's a good point. / I see it a little differently. | ★★★ | 70 |
| 4 | **提案・役割分担(グループワーク)** | Why don't we 〜? / I can take care of 〜. / Shall we split the tasks? | ★★★ | 70 |
| 5 | **懇親会・パーティ** | Cheers! / Is this seat taken? / What do you usually drink? / I'll get the next round. / It was great talking to you. | ★★★ | 80 |
| 6 | **質問・依頼・お願い** | Could you give me an example? / Would you mind sharing your screen? | ★★ | 50 |
| 7 | **進捗報告・発表** | Let me walk you through 〜. / We decided to 〜. / To sum up, 〜. | ★★ | 60 |
| 8 | **休憩中の雑談・文化の話題** | How's the training going for you? / What's your hometown like? / Have you tried natto? | ★★ | 60 |
| 9 | **会議進行・時間・トラブル対応** | We're running out of time. / Can everyone hear me? / Sorry to interrupt, but 〜. | ★ | 50 |

### 4.1 懇親会カテゴリ(#5)の内訳

懇親会は場面の流れに沿ってサブグループ化する(場面タグで管理):

| 場面タグ | 内容 | 例 |
|---|---|---|
| 輪に入る | 席・グループへの合流 | Mind if I join you? / Is this seat taken? |
| 乾杯・注文 | 乾杯、飲み物の話 | Cheers! / What are you having? |
| 飲まない選択 | 誘いの丁寧な断り方 | I'm good with soda, thanks. / I don't drink, but I love the atmosphere. |
| 食べ物の話 | 料理・食文化 | You should try this. / Is there anything you don't eat? |
| 相手を知る | 出身・仕事・プライベート | Where are you based? / What do you do outside work? |
| 盛り上げる | 相槌・リアクション・褒める | No way! / That's amazing. / Good one! |
| 連絡先交換 | SNS・今後のつながり | Are you on LinkedIn? / Let's keep in touch. |
| 切り上げる | 中座・退出の挨拶 | I'll grab another drink — catch you later. / It was great talking to you. |
| 翌朝の再会 | 2日目・3日目の朝、前夜会った人と | Good to see you again! / How was the rest of your night? / Did you sleep well? |
| 最終日の別れ | 3日目の締め・見送り | It was great meeting you all. / Have a safe trip home. / Hope we work together again. |

3日間開催なので、懇親会は一晩勝負ではない。初日は「輪に入る・相手を知る」、2日目は「再会・深掘り」、最終日は「別れ・つながりの維持」と役割が変わる。この流れを場面タグで練習できるようにする。

### 4.2 文化メモ(チートシートに併載)

フレーズだけでなく、多国籍の場で困らないための短い注意メモをチートシートに載せる:

- **飲まない人がいる前提で振る舞う**: インドネシア(ムスリムが多い)・インドなど、宗教や習慣でお酒や特定の食べ物(豚肉・牛肉など)を口にしない同僚がいる。飲み物・食べ物は勧めすぎず、"Is there anything you don't eat?" を先に。
- **安全な話題**: 食べ物、旅行、スポーツ、映画・ドラマ、出身地の見どころ、研修の感想。
- **避ける話題**: 政治(特に中国・香港・台湾の関係)、宗教、給料、年齢・結婚の詮索。
- **名前**: 多様な名前が飛び交う。聞き取れなければその場で "How do I pronounce your name?" と聞くのが最も失礼がない(カテゴリ2で練習)。

### 4.3 任意の文章追加

研修の案内メール・資料・当日聞き取れなかった表現をその場で登録し、同じSRSに乗せられる(§6)。

## 5. 学習フロー(2ヶ月カリキュラム)

### 5.1 1日の学習サイクル(15〜20分)

1. **復習**: SRSが「今日復習すべき」と判定した文章を出題(平均20〜40文)
2. **新規学習**: 新しい文章を1日 **10文**
3. 各文章の学習ステップ:
   - 日本語を見る → 英文を思い出して**口に出す**(想起練習)
   - 「答えを見る」で英文表示 → 🔊で音声を聞き **3回音読**
   - 自己評価: 「わからなかった / 微妙 / できた」

### 5.2 実カレンダーでの進行(2026年9月2日開始 → 11月2日研修)

学習開始から研修前日(11月1日)まで61日。8週間(56日)で新規560文を消化し、残り5日を新規ゼロの「直前スプリント」に充てる。

| 週 | 期間 | テーマ | 内容 |
|---|---|---|---|
| 1 | 9/2(水)〜9/8 | サバイバル | カテゴリ1(聞き返し・確認)+ カテゴリ2(自己紹介・名前)前半 |
| 2 | 9/9〜9/15 | 自分のことを話す | カテゴリ2後半 + カテゴリ6(質問・依頼) |
| 3 | 9/16〜9/22 | 議論に入る | カテゴリ3(意見・賛成・反対) |
| 4 | 9/23〜9/29 | グループワーク | カテゴリ4(提案・役割分担) |
| 5 | 9/30〜10/6 | 懇親会① | カテゴリ5前半(輪に入る〜相手を知る) |
| 6 | 10/7〜10/13 | 懇親会②+雑談 | カテゴリ5後半(盛り上げる〜最終日の別れ)+ カテゴリ8前半 |
| 7 | 10/14〜10/20 | 報告・発表 | カテゴリ7 + カテゴリ8後半 |
| 8 | 10/21〜10/27 | 進行・トラブル | カテゴリ9 |
| — | **10/28〜11/1** | **直前スプリント** | **新規ゼロ。**SRSの復習+★★★カテゴリの集中モード通し+チートシート読み込み |
| — | **11/2(月)〜11/4(水)** | **研修本番** | 朝: その日の場面(初日=自己紹介、2日目=再会、3日目=別れ)を集中モードで5分だけ予習。夜: 聞き取れなかった表現をその場で文章追加 |

- 懇親会カテゴリを5〜6週目(中盤)に置く理由: 序盤はまず研修の議論で使う土台(聞き返し・意見)を固める必要があり、一方で終盤に回すとSRSの定着サイクル(最長30日間隔)が11/1までに一巡しないため。
- 10文/日 × 56日 = 560文。数日サボっても直前スプリントの5日間がバッファになる。
- 研修本番の3日間もツールは使う前提(当日朝の集中モード予習と、夜の文章追加)。本番中の追加文は研修後の振り返り学習に残る。

### 5.3 間隔反復アルゴリズム(簡易ライトナー方式)

各文章は Box 0〜5 のいずれかに属し、自己評価で移動する。

- **できた** → Boxが1つ上がる
- **微妙** → Box据え置き(当日中に再出題)
- **わからなかった** → Box 0 に戻る(当日中に再出題)

| Box | 0 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|---|
| 次回まで | 当日 | 1日 | 3日 | 7日 | 14日 | 30日(定着扱い) |

Box 5 を2回連続「できた」なら **習得済み(mastered)**。

## 6. 機能一覧

### 6.1 MVP

1. **今日の学習** — 復習+新規10文(日本語→英語の想起型カード)
2. **音声読み上げ** — 速度 0.7x / 1.0x / 1.2x。en-AU / en-IN / en-GB / en-US 等をランダム切替するオプション(§3.1)
3. **自己評価とSRS** — 3段階評価でBoxを更新し、次回復習日を自動計算
4. **文章の追加・編集・削除** — 英文・日本語訳・カテゴリ・場面タグを登録。追加文も同じSRSに乗る
5. **デフォルト文章セット** — 研修+懇親会特化の約560文(§4の9カテゴリ)
6. **カテゴリ/場面 集中モード** — カテゴリや場面タグ(例: 「懇親会 > 輪に入る」)を指定して出題。「今夜は懇親会」という日の駆け込み練習用。SRSとは独立で、Boxは変更しない
7. **チートシートモード** — カテゴリ別フレーズ一覧(日英併記)+文化メモ(§4.2)。印刷向けCSS対応。研修・懇親会当日にスマホで見る/紙で持ち込む用
8. **進捗ダッシュボード** — 研修初日(11/2)までのカウントダウン、学習日数・ストリーク・習得済み文数・カテゴリ別定着率(★★★カテゴリの遅れを強調表示)。カレンダー計画(§5.2)に対する進み/遅れの表示
9. **本番中モード** — 研修期間(11/2〜11/4)は「今日の学習」の代わりに、その日の場面の集中モード予習と文章追加を前面に出す
10. **データのエクスポート/インポート**(JSON)

### 6.2 将来拡張(MVP後)

- 音声認識(SpeechRecognition)による発音チェック
- ミニダイアログモード(2〜4往復の会話形式。議論や懇親会の「流れ」の練習)
- 穴埋めモード(キーフレーズをブランクに)
- PWA化・リマインダー通知

## 7. 画面設計

SPA内のタブ切替で4画面(学習 / 文章一覧 / チートシート / 進捗)。

```
┌──────────────────────────────┐
│ 📚 研修英語   [学習] [一覧] [シート] [進捗] │
├──────────────────────────────┤
│ 研修まであと 42日   残り: 復習12 / 新規10   │
│  ┌────────────────────────┐  │
│  │ カテゴリ: 懇親会 > 輪に入る          │  │
│  │                                    │  │
│  │ 「ここ、座ってもいいですか?」        │  │
│  │        [ 答えを見る ]               │  │
│  └────────────────────────┘  │
│  ── 答え表示後 ──                          │
│  │ Mind if I join you?                 │  │
│  │ 🔊 0.7x/1x/1.2x  🎲音声ランダム      │  │
│  │ [わからなかった] [微妙] [できた]     │  │
└──────────────────────────────┘
```

- **文章一覧**: カテゴリ・場面タグでフィルタ+「+ 追加」フォーム。各行に編集・削除・Box表示。「この場面を集中練習」ボタン。
- **チートシート**: カテゴリ→フレーズ一覧(日英併記)+文化メモ+リスニング素材メモ。印刷用CSS対応。
- **進捗**: 研修日カウントダウン、ストリーク、カテゴリ別定着率バー、エクスポート/インポート。

## 8. データモデル

```js
// 文章(デフォルトはコード同梱、追加分は localStorage)
Sentence {
  id: string,          // "d-001"(デフォルト) / "u-<timestamp>"(ユーザー追加)
  en: string,          // "Mind if I join you?"
  ja: string,          // "ここ、座ってもいいですか?"
  category: string,    // §4 の9カテゴリ + "custom"
  scenes: string[],    // 場面タグ: "party-join" | "party-toast" | "discussion" | "presentation" など
  priority: number,    // サバイバル度 1-3(進捗画面の強調・総復習の優先度に使用)
  isCustom: boolean
}

// 学習進捗(localStorage: key = sentence id)
Progress {
  box: number,         // 0-5
  nextReview: string,  // "2026-09-03"(ISO日付)
  correctStreak: number,
  introduced: boolean
}

// アプリ状態(localStorage)
AppState {
  startDate: string,
  trainingDate: string,    // 研修初日。既定値 "2026-11-02"(カウントダウンの基準)
  trainingEndDate: string, // 研修最終日。既定値 "2026-11-04"(本番中モードの判定に使用)
  lastStudyDate: string,
  streak: number,
  newPerDay: number,       // 既定 10
  voiceVariety: boolean,   // 音声ランダム切替
  preferredVoices: string[] // 検出済みの利用可能音声(en-AU/en-IN/...)
}
```

localStorage キー: `eng.sentences.custom` / `eng.progress` / `eng.state`

## 9. ファイル構成

```
/
├── index.html      # アプリ本体(HTML + CSS + JS を1ファイルに同梱)
├── DESIGN.md       # 本設計書
└── README.md
```

※ デフォルト560文は `index.html` 内のJS配列として持つ。見通しが悪くなったら `sentences.js` に分離。

## 10. 主要ロジック(擬似コード)

```js
// 今日の出題キューを作る
function buildTodayQueue() {
  const due = allSentences.filter(s =>
    progress[s.id]?.introduced && progress[s.id].nextReview <= today());
  const fresh = allSentences.filter(s => !progress[s.id]?.introduced)
                            .slice(0, state.newPerDay);
  return shuffle(due).concat(fresh);   // 新規はカリキュラム順、復習はシャッフル
}

// 自己評価の反映
const INTERVALS = [0, 1, 3, 7, 14, 30]; // Box→日数
function grade(id, result) { // 'ng' | 'soso' | 'ok'
  const p = progress[id];
  if (result === 'ok')        p.box = Math.min(p.box + 1, 5), p.correctStreak++;
  else if (result === 'soso') requeueToday(id);
  else                        p.box = 0, p.correctStreak = 0, requeueToday(id);
  p.nextReview = addDays(today(), INTERVALS[p.box]);
  p.introduced = true;
  save();
}

// 集中モード(SRSに影響しない)
function drillMode(filter) {
  return shuffle(allSentences.filter(s =>
    s.category === filter.category ||
    s.scenes.includes(filter.scene)));
}

// 音声選択(ランダム切替オン時)
function pickVoice() {
  const targets = ['en-AU', 'en-NZ', 'en-IN', 'en-GB', 'en-US'];
  const available = speechSynthesis.getVoices()
    .filter(v => targets.some(t => v.lang.startsWith(t)));
  return state.voiceVariety ? randomOf(available) : defaultVoice(available);
}
```

## 11. 実装ロードマップ

| フェーズ | 期限 | 内容 | 完了条件 |
|---|---|---|---|
| 1 | **9/4まで** | 学習画面+SRS+localStorage(カテゴリ1の60文で動作確認) | 週1の学習をカレンダー通り開始できる |
| 2 | 9/8まで | 音声読み上げ(速度・音声ランダム切替)・文章追加/編集/削除・一覧画面 | 任意文章を追加してSRSに乗せられる |
| 3 | 9/15まで | デフォルト文章を560文に拡充(9カテゴリ+場面タグ) | 週3以降のカリキュラム分が揃う(週ごとの先行入力でも可) |
| 4 | 10月中旬まで | 集中モード・チートシート(文化メモ含む)・進捗画面・本番中モード | 直前スプリント(10/28〜)と当日運用の準備が整う |
| 5 | (任意) | GitHub Pages公開・スマホ最適化 | 通勤中にスマホで毎日使える |

フェーズ1〜3を最優先。**11/2は動かせないので、ツール作りに時間をかけすぎない**。フェーズ1(2〜3日)の完了と同時に週1カリキュラムの学習を始め、以降の機能は学習と並行して足していく。文章データはカリキュラム消化より1週先行していれば足りる。

## 12. 設計上の判断メモ

- **懇親会を独立カテゴリ(80文・★★★)にした理由**: 懇親会は日中のワークと環境が全く違う(騒音・カジュアル速度・広い話題・立食の出入り)。「研修英語のおまけ」では対応できず、輪への入り方から切り上げ方まで流れごと練習する必要がある。人間関係はむしろ夜の方が作られるので、投資対効果も高い。
- **懇親会カテゴリを5〜6週目に置いた理由**: 序盤に研修議論の土台を固めつつ、SRSの定着サイクル(最長30日間隔)が研修日までに一巡する位置に置いた。
- **「飲まない選択」を場面タグにした理由**: 参加国にインドネシア・インドが含まれ、宗教・習慣で飲まない同僚が確実にいる。自分が飲まない場合の断り方と、相手に強要しない振る舞いの両方をフレーズで押さえる。
- **文化メモをチートシートに載せる理由**: フレーズが完璧でも話題選びで失敗すると懇親会では致命的。特に政治の話題(中国・香港・台湾)は明示的に「避ける」と書いておく。
- **「聞き返し・確認」を最初に置く理由**: 研修・懇親会の両方で最も高頻度かつ、詰まった時の保険になる。騒がしい場で使う "Could you speak up?" 系もここに含める。
- **名前関連フレーズを厚くした理由**: 50人規模だと自己紹介と名前のやり取りが数十回発生する。9カ国の多様な名前を聞き返す・確認するフレーズは使用頻度が極めて高い。
- **旅行系フレーズを外した理由**: 60日×10文の枠は有限。研修1日の流れ(懇親会含む)に出てこない場面は収録しない。
- **想起(日→英)方向がデフォルト**: 必要なのは「言いたいこと→英文」の即時変換。
- **サーバーレス(localStorage)**: ユーザー1人・共有要件なし。導入の手間ゼロを優先し、消失リスクはJSONエクスポートで担保。

## 13. 実装状況(2026-09-02 時点)

MVP(§6.1)の機能はすべて実装済み。`index.html` + `sentences.js` の2ファイルで動作する。

| 機能 | 状態 |
|---|---|
| 今日の学習(復習+新規10文・想起型カード) | ✅ |
| 音声読み上げ(0.7x/1.0x/1.2x・音声ランダム切替) | ✅ |
| 自己評価とSRS(Box 0-5・当日再出題) | ✅ |
| 文章の追加・編集・削除(デフォルト文の「出題しない」設定含む) | ✅ |
| デフォルト文章セット | ⚠️ **405文**(目標560文に対して155文不足) |
| カテゴリ/場面 集中モード(SRS非干渉) | ✅ |
| チートシート(文化メモ・聞き取りの備え・印刷CSS) | ✅ |
| 進捗ダッシュボード(カウントダウン・計画比・カテゴリ別定着率) | ✅ |
| 本番中モード(11/2〜11/4の日別バナー) | ✅ |
| エクスポート/インポート(JSON) | ✅ |

### 文章数について

現在405文。10文/日で消化すると **41日分**(9/2開始なら10/12まで)。カリキュラム上は
10/27まで新規学習が続くので、**10月上旬までに約150文の追加が必要**。

不足は次のどちらかで埋める:

1. `sentences.js` の各カテゴリ配列の末尾に追記(まとめて足す場合)
2. 研修の案内メール・資料・日々の業務で出会った表現をアプリから随時追加

2 の方が定着しやすく、実際に使う可能性も高い。1日3〜4文足せば10月上旬に間に合う計算。

### 動作確認

Chromium(Playwright)で以下を確認済み:

- 405文の読み込み、新規10文の出題キュー生成
- 「できた」→ Box 1・翌日に再出題、「わからなかった」→ Box 0・当日中に再出題
- 集中モードが学習記録(Box)を変更しないこと
- 文章追加 → localStorage 保存 → SRS対象になること
- チートシートの場面別見出しと文化メモの表示
- 進捗のカウントダウン・カテゴリ別定着率(10カテゴリ)
- 研修当日バナー(日付を研修初日に設定して再現)
- リロード後も進捗が保持されること / JSエラーなし

### 既知の制約

- 読み上げは端末の音声合成に依存する。使える英語音声の種類はOS・ブラウザで異なり、
  en-NZ や en-IN が入っていない端末もある(利用可能な音声は学習画面に表示される)。
- 音声認識による発音チェック、ミニダイアログ、穴埋め、PWA化は §6.2 のとおり未実装。
