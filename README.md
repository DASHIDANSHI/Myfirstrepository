# 目的ランチャー (IntentLauncher)

スマホの使いすぎを防ぐための Android ホームランチャーです。
アプリを開く前に「**今、何をしたいですか？**」と目的をたずね、
その目的に割り当てたアプリだけを表示します。

## コンセプト

- ホーム画面をこのランチャーに置き換えます。
- ホームに戻るたびに、まず「目的」を選びます（連絡する／調べる／移動する…）。
- 目的を選ぶと、その目的に割り当てたアプリだけが並びます。
- **ランチャーに戻るたびに目的はリセット**されるので、毎回「何をしたいか」を意識してからアプリを開くことになります。これが衝動的な使いすぎを抑える仕組みです。
- 目的には任意で自由メモを添えられます（例:「上司に返信する」）。

## 主な画面

| 画面 | 役割 |
| --- | --- |
| 目的選択 (`GoalScreen`) | 「今、何をしたいですか？」＋目的カード一覧＋メモ欄 |
| アプリ一覧 (`AppListScreen`) | 選んだ目的に割り当てられたアプリだけを表示・起動 |
| 設定 (`SettingsScreen`) | 目的の追加・削除、各目的へのアプリ割り当て |

## 技術スタック

- Kotlin + Jetpack Compose (Material 3)
- DataStore Preferences + kotlinx.serialization（目的とアプリ割り当ての保存）
- `PackageManager` でインストール済みアプリを取得・起動
- minSdk 26 / targetSdk 34 / compileSdk 34

## ビルド方法

Android SDK が必要です（Android Studio 推奨）。

```bash
# 環境変数または local.properties で Android SDK の場所を指定
echo "sdk.dir=/path/to/Android/sdk" > local.properties

./gradlew assembleDebug
# 生成物: app/build/outputs/apk/debug/app-debug.apk
```

Android Studio で開く場合は、このリポジトリのルートを開けばそのまま実行できます。

## デフォルトのランチャーに設定する

1. 端末にインストール（`./gradlew installDebug` など）。
2. 端末の「設定 → アプリ → 標準アプリ → ホームアプリ」で「目的ランチャー」を選択。
3. ホームボタンを押すと目的選択画面が表示されます。

## 使い方

1. 初回はデフォルトの目的（連絡する／調べる/…）が入っています。
2. 右上の歯車（設定）から、各目的に起動を許可するアプリを割り当てます。
3. ホームで目的を選ぶ → その目的のアプリだけが表示され、そこから起動します。

## ホーム画面のレイアウト

- 上部に時計と「○月○日(曜)」の日付・曜日を表示。
- カテゴリボタン（8個）と、下段の「好きな画像」枠（2個）は同じ大きさで、
  すべて1画面に収まるよう各行を均等に配置（スクロール不要）。
- 画像枠をタップすると端末から画像を選んで表示できます（タップで選び直し）。

## がまんリスト（誘惑アプリ対策）

ツイッターやインスタなど「つい開いてしまうアプリ」は、目的ボタンには登録せず
**がまんリスト**に入れます。がまんリストのアプリは、わざと開きにくくしています。

開くまでの流れ（3つの「めんどくささ」）:

1. **検索しないと出てこない** — ボタンは無く、トップ画面で名前の一部（例: `insta`）を打って初めて候補が出る。
2. **本当に開く？の確認** — 候補をタップすると「本当に〇〇を開きますか？」と確認が出る。
3. **時間を先に決める** — 5／10／15分を選んでから開く。

そして **強制ブロック（ユーザー補助サービス）** をONにしておくと:

- 決めた時間が過ぎると、アプリを自動でホームに戻して閉じます。
- 手順を踏まずに（通知や履歴から）開いた場合も、すぐホームに戻します。

### 強制ブロックの有効化

1. アプリの「設定」を開く → 上部の「強制ブロック」カードの「ユーザー補助の設定を開く」を押す。
2. 端末の「設定 → ユーザー補助」で「目的ランチャー 強制ブロック」をON。
3. 「画面を操作・監視します」という警告が出ますが、これは仕組み上必要なものです（画面の中身は読み取りません。前面アプリの種類だけを見ています）。

> 補足: アクセシビリティサービスは前面アプリの「パッケージ名」だけを使い、画面内容は取得しません（`canRetrieveWindowContent=false`）。

## 権限について

- `QUERY_ALL_PACKAGES`: インストール済みアプリを一覧して目的に割り当てるために使用します。個人利用・サイドロード前提の権限で、Google Play で配布する場合はポリシー対応が別途必要です。
- アクセシビリティサービス（`BIND_ACCESSIBILITY_SERVICE`）: 強制ブロックのために、前面アプリの種類を監視します。ユーザーが手動でONにしたときだけ動きます。

## 今後の拡張案

- アプリごとに「あいことば」を設定して、正確に打たないと開けない厳しめモード
- 1日の振り返り（どの目的・どのがまんアプリをどれだけ使ったか）の記録
- ブロック時に専用のクッション画面（深呼吸メッセージ）を表示
